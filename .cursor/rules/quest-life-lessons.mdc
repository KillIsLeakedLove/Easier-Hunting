---
description: 每轮任务前读 QUEST_LIFE_LESSONS.md，失败后必须追加记录
alwaysApply: true
---

# 任务经验：先读后记

本仓库**每一轮任务**都必须遵守（不限于倒下次数脚本）：

1. **执行前**先完整阅读仓库根目录 `QUEST_LIFE_LESSONS.md`（硬性禁止表 + 最近失败）。未读不得改代码、不得打包。
2. **失败后**（界面不对、闪退、卡死、黑屏、或测试判定失败）：先把版本、日志关键行、玩家看到的结果、教训 **追加** 进该文件，再开始下一轮。不准跳过记录直接改下一版。
3. 禁止重复该文件「硬性禁止」表中的做法。
4. overlay 的 `after=99` / `Quest list patched` **不等于**成功。成功只看游戏界面是否显示「力尽倒下99次」。
5. 在已 hook 的方法内部 **禁止再调用同名方法**（1.7.44：`get_QuestLife` 里再 `call get_QuestLife` → Recursive hook，进任务列表卡死）。
